# AsReX pipeline dependencies -------------------------------------------------
# CUDA 12.1 wheels (remove the extra-index line if installing CPU builds)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchaudio==2.3.1+cu121

# Core models
asteroid==0.7.0                     # ConvTasNet separation (via Asteroid)
whisperx==3.3.1                     # Word-level alignment
reazonspeech-espnet-asr==2.1.0      # ESPnet-based Japanese ASR
reazonspeech-nemo-asr==2.1.0        # NeMo-based Japanese ASR

# Audio tooling
librosa>=0.10,<0.11
soundfile>=0.12
numpy==1.26.4

# Utilities
tqdm>=4.66
python-dotenv>=1.0
Cython>=3.0
PyYAML>=6.0
